yamlApplicationConfig:
  kafka:
    clusters:
      - name: kafka-cluster
        bootstrapServers: kafka-service.kafka.svc.cluster.local:9092
    auth:
      type: disabled
  management:
    health:
      ldap:
        enabled: false
